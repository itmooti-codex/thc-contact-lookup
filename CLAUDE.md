# thc-contact-lookup — The Happy Clinic Ontraport App

See the main QueryBuilder CLAUDE.md for full framework documentation.

## Project Details
- **Client:** The Happy Clinic (slug: `thc`)
- **Type:** Ontraport App (vanilla JS, no build step)
- **GitHub Pages:** https://itmooti-codex.github.io/thc-contact-lookup
- **VitalSync SDK:** Loaded via CDN in header code
- **Script Load Order:** config.js → utils.js → models.js → vitalsync.js → app.js

## Quick Commands
- `npm run dev` — Start local dev server
- `npm run parse-schema` — Re-generate types + schema reference from schema.xml

## Schema Workflow
1. Export schema XML from VitalStats and place at `schema/schema.xml`
2. Run `npm run parse-schema`
3. This generates:
   - `src/types/models.js` — JSDoc typedefs + MODELS metadata
   - `schema/schema-reference.json` — Full parsed schema reference
   - Updates the Schema Reference section below

<!-- SCHEMA:START — Auto-generated by parse-schema.js. Do not edit manually. -->
## Schema Reference

37 tables, 1108 columns. Full details: `schema/schema-reference.json`

### Channel (SDK: `ThcChannel`) — 3 fields
**Other:** id(PK), created_at(ts), last_modified_at(ts)

### Coupon (SDK: `ThcCoupon`) — 21 fields
**Other:** id(PK), coupon_code, coupon_name, created_at(ts), deleted(bool), description, discount_type(enum:Flat|Percent|Trial), discount_value(float), end_at(ts), issued, new_buyers, product_selection(enum:all|specific), recurring(bool), redeemed_count, remaining_count, start_at(ts), status(enum:Expired|Not Valid|Reached Limit|Valid), total_amount_collected(currency), type(enum:Group|Personal), valid_timeframe, valid_type

### Gateway (SDK: `ThcGateway`) — 7 fields
**Other:** id(PK), created_at(ts), external_id, is_default(bool), name, status(enum:Disconnected|Enabled|Test Mode), type(enum:21 values)

### LandingPage (SDK: `ThcLandingPage`) — 50 fields
**Other:** id(PK), a_convert, a_sent, autosave(longtext), b_convert, b_sent, c_convert, c_sent, conversion_rate(percent), conversions, convert_0, convert_1, convert_2, convert_3, created_at(ts), d_convert, d_sent, domain(url), last_auto(ts), last_modified_at(ts), last_save(ts), name, resource(json), rotation, seo_settings, ssl_enabled, type(enum:Code Mode|Legacy Pages|ONTRApages|ONTRApages Legacy|Redirect), unique_a_convert, unique_a_sent, unique_b_convert, unique_b_sent, unique_c_convert, unique_c_sent, unique_convert_0, unique_convert_1, unique_convert_2, unique_convert_3, unique_d_convert, unique_d_sent, unique_visits, unique_visits_0, unique_visits_1, unique_visits_2, unique_visits_3, uri_id, visits, visits_0, visits_1, visits_2, visits_3

### MessageTemplate (SDK: `ThcMessageTemplate`) — 12 fields
**Other:** id(PK), created_at(ts), deletable(bool), inline, json_data, last_modified_at(ts), last_sent, name, resource, sent, status(enum:Approved|Not Submitted|Rejected|Submitted for Review), type(enum:Double Opt-In (ONTRAMail)|Double Opt-In (legacy)|Invoice (ONTRAMail)|Invoice (legacy))

### ObjectLogEntry (SDK: `ThcObjectLogEntry`) — 27 fields
**Other:** id(PK), Appointment_id(FK→Appointment), BlogComment_id(FK→BlogComment), BlogPost_id(FK→BlogPost), CanceledSubscription_id(FK→CanceledSubscription), ClinicalNote_id(FK→ClinicalNote), Contact_id(FK→Contact), Conversation_id(FK→Conversation), Dispense_id(FK→Dispense), Item_id(FK→Item), OPatientInterestedPatientInterestedItem_id(FK→OPatientInterestedPatientInterestedItem), Script_id(FK→Script), Timeslot_id(FK→Timeslot), User_id(FK→User), message_id(FK→Message), created_at(ts), details(longtext), entry_items_count, merge_data(json), object_id, object_type_id, split_num, status(enum:15 values), step_num, subject, type(enum:26 values), vtype

### ObjectLogEntryItem (SDK: `ThcObjectLogEntryItem`) — 13 fields
**Other:** id(PK), message_id(FK→Message), object_log_entry_id(FK→ObjectLogEntry), action_id, created_at(ts), details, item_order, merge_data(json), object_type_id, resource, status(enum:15 values), type(enum:26 values), vtype

### OntraportListFieldOption (SDK: `ThcOntraportListFieldOption`) — 5 fields
**Other:** id(PK), description, fieldId, name, objectId

### Order (SDK: `ThcOrder`) — 2 fields
**Other:** id(PK), last_modified_at(ts)

### Product (SDK: `ThcProduct`) — 13 fields
**Other:** id(PK), created_at(ts), deleted(bool), description, external_id, internal_name, last_modified_at(ts), price(currency), product_group, public_name, revenue(currency), sku, total_purchases_count

### Subscription (SDK: `ThcSubscription`) — 2 fields
**Other:** id(PK), created_at(ts)

### Tag (SDK: `ThcTag`) — 3 fields
**Other:** tag_id(PK), name, object_type_id

### User (SDK: `ThcUser`) — 1 fields
**Other:** id(PK)

### BlogPost (SDK: `ThcBlogPost`) — 41 fields
**System Information:** id(PK), owner_id(FK→User), last_note, unique_id, created_at(ts), ip_address, last_activity(ts), last_sms_sent(ts), profile_image(imageUrl), last_email_sent(ts), last_call_logged(ts), last_modified_at(ts), last_sms_received(ts), last_email_received(ts)
**Post Settings:** likes, post_date(ts), author_bio(longtext), post_title, author_name, post_status(enum:Idea Backlog|Published|Review|Writing), author_image(imageUrl), time_to_read, editor_s_pick(bool), post_deadline(ts), post_url_slug
**Post Publish Settings:** default_blog_post_page_type_url(url), default_blog_post_page_type_visits, default_blog_post_page_type_published(bool), default_blog_post_page_type_unique_visits
**Post Content:** headline, subheadline, post_content, main_post_image(imageUrl), short_post_description(longtext)
**SEO Settings:** seo_page_title, seo_page_description(longtext), social_image_2_1_ratio(imageUrl)
**Blog post editor:** blog_post_editor_url(url), blog_post_editor_visits, blog_post_editor_published(bool), blog_post_editor_unique_visits

### CouponProduct (SDK: `ThcCouponProduct`) — 4 fields
**Other:** id(PK), coupon_id(FK→Coupon), product_id(FK→Product), product_name

### Message (SDK: `ThcMessage`) — 33 fields
**Other:** id(PK), owner_id(FK→User), autosave, clicked_count, complaints_count, created_at(ts), from, json_data(longtext), last_auto(ts), last_modified_at(ts), last_save(ts), message_body(longtext), name, not_clicked_count, not_opened_count, object_type_id, old_resource(longtext), opened_count, plaintext(longtext), reply_to_email, resource(json), send_from_email, send_out_name, send_to, sent_count, site_id, spam_score(float), subject, transactional_email(bool), type(enum:10 values), unsubscribed_count, utm_tracking, word_wrap_checkbox

### Note (SDK: `ThcNote`) — 21 fields
**Other:** id(PK), Appointment_id(FK→Appointment), BlogComment_id(FK→BlogComment), BlogPost_id(FK→BlogPost), CanceledSubscription_id(FK→CanceledSubscription), ClinicalNote_id(FK→ClinicalNote), Contact_id(FK→Contact), Conversation_id(FK→Conversation), Dispense_id(FK→Dispense), Item_id(FK→Item), OPatientInterestedPatientInterestedItem_id(FK→OPatientInterestedPatientInterestedItem), Script_id(FK→Script), Timeslot_id(FK→Timeslot), User_id(FK→User), author_id(FK→User), date_created(ts), last_modified_at(ts), note(longtext), object_id, object_type_id, type(enum:API|Form|Incoming|Manual|Outgoing)

### TagSubscriber (SDK: `ThcTagSubscriber`) — 17 fields
**Other:** id(PK), Appointment_id(FK→Appointment), BlogComment_id(FK→BlogComment), BlogPost_id(FK→BlogPost), CanceledSubscription_id(FK→CanceledSubscription), ClinicalNote_id(FK→ClinicalNote), Contact_id(FK→Contact), Conversation_id(FK→Conversation), Dispense_id(FK→Dispense), Item_id(FK→Item), OPatientInterestedPatientInterestedItem_id(FK→OPatientInterestedPatientInterestedItem), Script_id(FK→Script), Timeslot_id(FK→Timeslot), User_id(FK→User), tag_id(FK→Tag), object_id, object_type_id

### Contact (SDK: `ThcContact`) — 299 fields
**System Information:** id(PK), spent, trigger, facebook, linkedin, timezone(tz), dialpadid, instagram, last_note, unique_id, created_at(ts), ip_address, user_agent, last_activity(ts), last_sms_sent(ts), last_email_sent(ts), last_call_logged(ts), last_modified_at(ts), last_sms_received(ts), last_email_received(ts)
**Contact Information:** age, fax(phone), city, email(email), state(ISO 3166-2 code as string without the country prefix. Ex: US-FL = FL), title, address, company, country(countryCode), website(url), birthday(ts), state_au(enum:8 values), zip_code, address_2, last_name, coordinate(geoPoint), first_name, sms_number, address_lat(lat), address_long(lng), office_phone, profile_image(imageUrl), address_validation(enum:Error|Get Validated|Invalid Address|Not Validated|Validated)
**Lead Information:** gclid, score, owner_id(FK→User), priority(enum:High|Low|Medium), sales_stage(enum:7 values), facebook_fbc, facebook_fbp, ga_client_id, ga_session_id, here_about_us(enum:Google Search|Other|Print/Newspaper|Radio|Social Media|Word of Mouth), referring_page, sms_permission(bool), bulk_sms_status(bool), last_referrer_id(FK→Contact), bulk_email_status(bool), first_referrer_id(FK→Contact), ga_session_number, linked_in_click_id, time_since_last_activity(enum:More than a month ago (Cold)|This Week|This month|Today (Hot!))
**SMS Merge Fields:** last_inbound_sms
**Invoice Info:** last_invoice, last_dispense_id, last_charge_amount(currency), last_total_invoice_amount(currency), total_amount_of_unpaid_transactions(currency)
**Last Credit Card Used:** last_card_type(enum:7 values), last_cc_status(enum:[object Object]|Declined|Success), last_card_number_last_4, last_card_expiration_date(ts), last_card_expiration_year, last_card_expiration_month(enum:12 values)
**Team Information:** bsb, team_role(enum:7 values), entity_name, pharmacy_abn, date_first_logged_in(ts)
**PATIENT: Application:** patient_application_url(url)
**Booked Appointments:** total_thc_margin, last_appointment_id, future_appointments_booked, last_appointment_booked_time(ts)
**Application Information:** application_date(ts), terms_conditions(bool), time_signed_terms(ts), application_status(enum:10 values)
**Application Fields:** sex(enum:Female|Intersex|Male|Prefer not to say)
**Eligibility Quiz Completion:** jotform_id(longtext), treatment_outcome(longtext), previous_treatment(longtext), long_term_condition(longtext), time_completed_quiz(ts), mental_health_history(longtext), pregnancy_or_fertility(longtext)
**Pharmacy Information:** gateway_id(longtext), dispenses_all, pharmacy_logo(imageUrl), pharmacy_name, pharmacy_phone, pharmacy_state, pharmacy_suburb, pharmacy_address, dispenses_on_hold, dispenses_delivered, invoice_template_id(longtext), dispenses_in_transit, pharmacy_postal_code(address), dispenses_in_progress, dispenses_tracking_added, dispenses_to_be_confirmed
**Dr Information:** halaxy_email(email), practitioner, clinician_status(enum:9 values), link_to_tga_docs(url), clinician_end_away(ts), available_timeslots, clinician_start_away(ts), available_appointments, appointments_left_today, incomplete_appointments, clinician_min_weekly_time_slots
**PHARMACY: Dispense Requests:** pharmacy_dispense_requests_url(url)
**Existing Conditions:** adhd(bool), ptsd(bool), cancer(bool), epilepsy(bool), glaucoma(bool), arthritis(bool), headaches(bool), migraines(bool), depression(bool), fibromyalgia(bool), inflammation(bool), endometriosis(bool), sleep_disorder(bool), chronic_illness(bool), other_condition(longtext), palliative_care(bool), anxiety_disorder(bool), loss_of_appetite(bool), neuropathic_pain(bool), multiple_sclerosis(bool), parkinson_s_disease(bool), allergies_information, chronic_non_cancer_pain(bool), autism_spectrum_disorder(bool), crohns_ulcerative_colitis_ibs_gut(bool), chemotherapy_induced_nausea_and_vomiting(bool)
**Other Application Information:** clinic_name, tobacco_smoker(enum:No|Yes), discharge_letter(json), using_other_clinic(enum:I am currently using another natural therapy clinic|I am not currently using another natural therapy clinic), alternative_clinic_email(email), do_you_consent_to_sending_a_notice_of_discharge_to_your_current_(bool)
**Confirm If True:** none_of_these_apply_to_me(bool), i_have_an_allergy_to_cannabinoids(bool), i_suffer_from_chronic_liver_disease(bool), i_am_currently_pregnant_or_breastfeeding(bool), i_have_a_history_of_suicidal_ideations_and_or_self_harm(bool), i_have_a_history_of_schizophrenia_bipolar_and_or_psychosis(bool), history_of_opioid_replacement_therapy_and_or_drug_dependency(bool)
**Medicard Number:** irn, ihi_number, issue_number, medicare_name, valid_to_year(enum:11 values), valid_to_month(enum:12 values), medicare_number, halaxyid_patient, medicare_not_validating(bool), veteran_healthcare_card_holder(bool)
**Shipping Address Information:** oils(bool), vapes(bool), edibles(bool), flowers(bool), business_name, budget_important(bool), effect_preference(enum:Faster Onset|Longer Lasting), prev_cannabis_use(bool), lineage_preference(enum:Balanced|Indica|Sativa), product_preference(enum:Balanced|Higher CBD|Higher THC), discretion_important(bool), confirm_business_opening_hours(longtext), shipping_address_is_a_business(bool)
**Customer Contact:** contact_comment(longtext)
**Partner Data:** owed, leads, visits, total_sales, total_refunds, paypal_address, number_of_sales, number_of_refunds, total_commission_paid, active_referred_open_orders
**Partner Center: Home:** partner_center_home_url(url)
**My Account app | My account page:** my_account_app_my_account_page_url(url)
**Document Signed:** signature(imageUrl), document_link(url), date_signed_document(ts), document_signing_url(url), document_signing_visits, document_signing_published(bool), document_signing_unique_visits, declaration_i_have_answered_truthfully(bool)
**Dr Appointment Stats:** dr_appt_paid, dr_appt_count_all, dr_appt_cancelled_count, dr_appt_awaiting_payment_billed, dr_appointment_pending_completion, dr_appt_awaiting_payment_unbilled
**PATIENT Eligibility:** patient_eligibility_url(url)
**Dr Payment Stats:** dr_bill_date(ts), doctor_bill_id, dr_bill_status(enum:Bill Created|Bill Sent|Run Payment), dr_pay_reference, dr_payments_paid, dr_payments_billed, dr_payments_pending, dr_payments_un_billed, doctor_payment_pending(currency)
**Doctor Expression of Interest:** hpi_i, expertise(longtext), ahpra_number, availability(longtext), qualifications(longtext), date_applied_eoi(ts), how_soon_to_start(enum:Immediately|In a couple of months|Not available for a while|Within a few weeks), doctor_public_blurb, authorised_prescriber(bool), pbs_prescriber_number, professional_description(longtext), patient_focus_options_as_text, experience_prescribing_cannabis(enum:An Expert|Minimal Experience|No Experience|Some Experience), registration_authority_ra_number, why_are_you_interested_in_working_with_us(longtext)
**DOCTOR: EOI:** doctor_eoi_url(url)
**Script information:** open_scripts, scripts_open, treatment_plan(enum:7 values), date_last_script(ts), scripts_archived, date_first_script(ts), scripts_fulfilled, last_doctor_seen_id(FK→Contact)
**Nurse Evaluation:** ai_notes, ai_consultation(bool), cannabis_outcome(enum:Eligible|Ineligible|Unkown), date_ai_consulation(ts)
**Item Purchases:** date_last_item_shipped(ts), date_last_item_purchase(ts), date_first_item_purchase(ts)
**cart Information:** cart_gst(currency), cart_status(enum:Clear|Open|Payment Failed|Payment Success|Process|Processing), cart_shipping(currency), cart_subtotal, calculate_cart(bool), cart_items_gst, shipping_items, cart_grand_total(currency), cart_items_total, processing_result(longtext), cart_gst_percentage, cart_credit_card_fee(currency), preferred_pharmacy_id(FK→Contact), cart_credit_card_fee_gst(currency)
**API ERRORS:** api_error(longtext)
**XOSync - XERO CONTACT INFORMATION:** xero_contact_key
**RERERRER: Gift a Friend:** rererrer_gift_a_friend_url(url)
**Medications:** what_is_working_for_you(longtext), list_your_medications_supplements(longtext), why_regular_medicine_isn_t_working(longtext), are_you_currently_taking_any_medications_or_supplements(enum:No|Yes)
**Feedback Tracking:** feedback_send_count, last_feedback_rating, last_time_feedback_request_sent(ts)
**Parcel Locker:** shipping_option(enum:Home Address|Parcel Locker), parcel_locker_city(address), parcel_locker_state(enum:8 values), parcel_locker_number, parcel_locker_street(address), parcel_locker_postal_code(address)
**Manual SMS Message:** action, reasoning(longtext), manual_sms(enum:Send|Sent), manual_email(enum:Send|Sent), sms_message_to_send(longtext), email_message_to_send(longtext), email_subject_to_send
**Referral From Items:** referrer_commission(bool), last_date_referrer_paid(ts)
**Coupon Settings:** switch_coupon(enum:$40 Off Coupon Sent|$40 Off Coupon Used)
**Flower Limit Information:** check_flower_limit(bool), flower_gms_in_cart, flower_gms_available, flower_limit_reached(bool), monthly_cannabis_dispense_limit

### BlogComment (SDK: `ThcBlogComment`) — 18 fields
**System Information:** id(PK), owner_id(FK→User), last_note, unique_id, ip_address, last_activity(ts), last_sms_sent(ts), profile_image(imageUrl), last_email_sent(ts), last_call_logged(ts), last_modified_at(ts), last_sms_received(ts), last_email_received(ts)
**Blog Comment Info:** status(enum:Approved|New|Spam), comment(longtext), created_at(ts), blog_post_id(FK→BlogPost), commenter_id(FK→Contact)

### CanceledSubscription (SDK: `ThcCanceledSubscription`) — 18 fields
**System Information:** id(PK), owner_id(FK→User), last_note, unique_id, created_at(ts), ip_address, last_activity(ts), last_sms_sent(ts), profile_image(imageUrl), last_email_sent(ts), last_call_logged(ts), last_modified_at(ts), last_sms_received(ts), last_email_received(ts)
**Canceled Subscription Information:** product_canceled, reason_for_canceling(longtext), cancellation_feedback(longtext), contact_who_canceled_id(FK→Contact)

### ContactPatientFocusOption (SDK: `ThcContactPatientFocusOption`) — 3 fields
**Other:** id(PK), optionId(FK→OntraportListFieldOption), recordId(FK→Contact)

### Conversation (SDK: `ThcConversation`) — 39 fields
**Other:** id(PK), assigned_to_id(FK→User), channel_id(FK→Channel), contact_id(FK→Contact), bindex, bulk_mail(enum:Double opt-in|Hard bounce|Opted-in|Transactional Only|Under review), bulk_sms(enum:Hard bounced|One to one only|Opted-in|Pending confirmation|Pending review|Unsubscribed), contact_send_to, conversation_status(enum:Closed|Open), created_at(ts), date_closed(ts), draft, import_id, initiator, ip_address, ip_addy_display, is_read(bool), last_activity(ts), last_call_logged(ts), last_email_received(ts), last_email_sent(ts), last_modified_at(ts), last_note, last_read, last_sms_received(ts), last_sms_sent(ts), minutes_since_last_inbound_message, minutes_since_last_outbound_message, perm_closed(bool), profile_image(imageUrl), send_field(enum:SMS Number), sla_warning(enum:High|Low|Medium|Urgent), source_location, spam(bool), system_source, time_to_close, time_to_first_response, title, unique_id

### CouponCode (SDK: `ThcCouponCode`) — 7 fields
**Other:** id(PK), contact_id(FK→Contact), code, created_at(ts), deleted(bool), expires_at(ts), redeemed_at(ts)

### CreditCard (SDK: `ThcCreditCard`) — 16 fields
**Other:** id(PK), contact_id(FK→Contact), billing_address, billing_address_2, billing_city, billing_country(countryCode), billing_first_name, billing_last_name, billing_state(ISO 3166-2 code as string without the country prefix. Ex: US-FL = FL), billing_zip, card_expiration_month, card_expiration_year, card_number_last_4, card_status(enum:Active|Active (Default)|Expired|Expired (Default)|Hidden), card_type(enum:AMEX|Discover|Mastercard|Other|Visa), last_charge_at(ts)

### Item (SDK: `ThcItem`) — 162 fields
**System Information:** id(PK), owner_id(FK→User), last_note, unique_id, created_at(ts), ip_address, last_activity(ts), last_sms_sent(ts), profile_image(imageUrl), last_email_sent(ts), dominant_terpene, last_call_logged(ts), last_modified_at(ts), last_sms_received(ts), last_email_received(ts)
**Item Information:** gst(bool), type(enum:Accessory|Edible|Extract|Flower|Oil|Vape), brand, expiry(ts), status(enum:In Stock|Unavailable), carrier(enum:Coconut Oil|Hemp Oil|Hemp Seed Oil|MCT Oil|Olive Oil|Other), sub_type(enum:Budget|Core|Craft|Vaporiser), dominance(enum:8 values), item_name, pack_size, item_image(imageUrl), retail_gst(currency), description(longtext), item_image_2(imageUrl), retail_price(currency), quantity_unit(enum:g|mg|mls|pieces|units), packaging_date(ts), wholesale_price(currency), information_sheet(json), market_entry_date(ts), concession_discount(currency), estimated_date_in_stock(ts)
**ITem Details:** chemovar, onset_time(enum:15–45 min|1–2 hrs|1–5 min|30–90 min|Variable), dosage_form(enum:Inhalation (Smoking/Vaping)|Oral (Capsules, Edibles)|Sublingual (Tinctures, Sprays)|Suppository (Rectal/Vaginal)|Topical (Creams, Lotions)|Transdermal Patch), other_notes, typical_use, sativa_indica(enum:11 values), expected_effect, typical_duration(enum:1–4 hrs|4–6 hrs|6–10 hrs|6–12 hrs|6–8 hrs|Localised only)
**Strength:** cbc, cbd, cbg, cbn, thc, tga_category(enum:1|2|3|4|5), tga_schedule, strength_unit(enum:8 values), link_to_catalyst_listing(url)
**BEnefits and Conditions:** terpene_profile(enum:High >4%|Low <2%|Medium 2-4%), benefits_options_as_text, conditions_options_as_text
**Formulary Information:** cmi(url), prioritise(bool), high_profit(bool), paul_rating, psychoactive(enum:Mild/Moderate|No|Yes), update_preferences(bool), client_preference_effect(enum:Faster Onset|Longer Lasting), client_preference_lineage(enum:Balanced|Indica|Sativa), client_preference_thc_cbd(enum:Balanced|Higher CBD|Higher THC)
**Shipping Details:** sku, width, height, length, weight
**Halaxy Information:** form(enum:21 values), route(enum:10 values), strength_1, strength_2, strength_3, manufacturer, strength_unit_1(enum:%|g|mg|mg/g|mg/ml), strength_unit_2(enum:%|g|mg|mg/g|mg/ml), strength_unit_3(enum:%|g|mg|mg/g|mg/ml), active_ingredient_1(enum:cannabidiolic acid (cbda)|cannabigerol (cbg)|cannabinol (cbn)|delta-9-tetrahydrocannabinol), active_ingredient_2(enum:cannabidiolic acid (cbda)|cannabigerol (cbg)|cannabinol (cbn)|delta-9-tetrahydrocannabinol), active_ingredient_3(enum:cannabidiolic acid (cbda)|cannabigerol (cbg)|cannabinol (cbn)|delta-9-tetrahydrocannabinol), dosage_instructions(longtext)
**Item Profile Page:** item_profile_page_url(url), item_profile_page_visits, item_profile_page_published(bool), item_profile_page_unique_visits
**Terpenes:** guaiol, others, phytol, borneol, fenchyl, myrcene, ocimene, terpene, camphene, humulene, limonene, linalool, farnesene, nerolidol, terpineol, beta_pinene, terpinolene, alpha_pinene, delta_3_carene, alpha_bisabolol, trans_nerolidol, beta_caryophyllene, caryophyllene_oxide, trans_caryophyllene, dominant_terpenes_options_as_text
**Item Detail Page:** item_detail_page_url(url), item_detail_page_visits, item_detail_page_published(bool), item_detail_page_unique_visits
**Growing Conditions:** cured(bool), organic(bool), packaging(enum:10 values), cured_text, trim_method(enum:Hand|Hybrid|Machine|Other|Unknown), light_source(enum:Artificial light|Hybrid light source|Natural light source|Unknown), growing_medium(enum:8 values), harvest_method(enum:Hand|Hybrid|Machine|Other|Unknown), origin_country(countryCode)
**Last Time Checked Catalyst:** catalyst_checked(bool), last_time_checked(ts)
**Shop:** product_id, item_not_shipped(bool), show_on_public_store(bool)
**Storefront Product Detail Page:** storefront_product_detail_page_url(url), storefront_product_detail_page_visits, storefront_product_detail_page_published(bool), storefront_product_detail_page_unique_visits
**Calculated Pricing:** profit, price_per_g(currency), expiry_score, gross_profit(currency), price_per_mg(currency), pharmacy_deal, wholesaler_id(FK→Contact), formulary_deal, calculate_price_per_mg(bool), pharmacy_for_dispatch_id(FK→Contact)
**Script Information:** script_count
**Condition Rating:** chronic_pain
**Stock Management:** id_2, notes(longtext), check_stock(bool), price_cents, product_name, product_type, cannabis_type, tga_schedule_2, tga_sas_category, brisbane_in_stock(bool), brisbane_is_active(bool), melbourne_in_stock(bool), melbourne_is_active(bool), product_size_amount, product_size_measure

### Timeslot (SDK: `ThcTimeslot`) — 25 fields
**System Information:** id(PK), trigger, last_note, unique_id, created_at(ts), last_sms_sent(ts), profile_image(imageUrl), last_modified_at(ts), last_sms_received(ts)
**Timeslot Information:** end_time(ts), doctor_id(FK→Contact), start_time(ts), appointments, timeslot_status(enum:Cancelled|Closed For Appointments|Completed Timeslot|Open For Appointments), max_appointments, percent_completed, available_appointments, completed_appointments, appointments_left_today, incomplete_appointments
**DOCTOR: Timeslot View:** doctor_timeslot_view_url(url)
**Timeslot Finance:** doctor_cost, appointment_fees, total_thc_margin, total_retail_revenue

### Appointment (SDK: `ThcAppointment`) — 55 fields
**System Information:** id(PK), halaxyid, owner_id(FK→User), last_note, unique_id, created_at(ts), ip_address, last_activity(ts), last_sms_sent(ts), profile_image(imageUrl), last_email_sent(ts), last_call_logged(ts), last_modified_at(ts), last_sms_received(ts), last_email_received(ts)
**Appointment Information:** paid(bool), type(enum:Follow Up Consultation|Initial Consultation), order, status(enum:7 values), fee_paid(currency), date_paid(ts), doctor_id(FK→Contact), patient_id(FK→Contact), date_booked(ts), timeslot_id(FK→Timeslot), script_count, draft_scripts, appointment_time(ts), date_rescheduled(ts), patient_comments(longtext), patient_didn_t_show(bool), timeslot_start_time(ts)
**PATIENT: Appointment Status:** patient_appointment_status_url(url), patient_appointment_status_visits, patient_appointment_status_published(bool), patient_appointment_status_unique_visits
**DOCTOR: Appointment View:** doctor_appointment_view_url(url), doctor_appointment_view_visits, doctor_appointment_view_published(bool), doctor_appointment_view_unique_visits
**Doctor Payment Information:** date_doctor_paid(ts), dr_pay_reference, id_billing_invoice, date_billing_invoice(ts), doctor_payment_status(enum:Awaiting Payment - Billed|Awaiting Payment - Unbilled|Paid|Pending Completion|Sent to Xero), doctor_consultation_fee(currency)
**Internal Notes Section:** conditions(longtext), process_steps(enum:1. Ask Preferences|2. Select Medicine(s)|3. Open Halaxy and Process ERX|4. Close Appointment|5. Completed), additional_notes(longtext), triage_nurse_notes
**AI Processing Fields:** condition(longtext), conditions_to_support
**Script Finance:** total_thc_margin(currency), total_retail_revenue
**Formulary Information:** failed_message

### Invoice (SDK: `ThcInvoice`) — 28 fields
**Other:** id(PK), contact_id(FK→Contact), credit_card_id(FK→CreditCard), gateway_external_id, landing_page_id(FK→LandingPage), order_id(FK→Order), contact_name, created_at(ts), external_order_id, hidden(bool), last_modified_at(ts), last_recharged_at(ts), offer_data, recharge(bool), recharge_attempts, shipping_amount(currency), status(enum:8 values), subtotal(currency), tax_city, tax_city_amount(currency), tax_county, tax_county_amount(currency), tax_postal_code, tax_state, tax_state_amount(currency), template_id, total(currency), total_tax(currency)

### ItemBenefitsOption (SDK: `ThcItemBenefitsOption`) — 3 fields
**Other:** id(PK), optionId(FK→OntraportListFieldOption), recordId(FK→Item)

### ItemConditionsOption (SDK: `ThcItemConditionsOption`) — 3 fields
**Other:** id(PK), optionId(FK→OntraportListFieldOption), recordId(FK→Item)

### ItemDominantTerpenesOption (SDK: `ThcItemDominantTerpenesOption`) — 3 fields
**Other:** id(PK), optionId(FK→OntraportListFieldOption), recordId(FK→Item)

### OPatientInterestedPatientInterestedItem (SDK: `ThcOPatientInterestedPatientInterestedItem`) — 5 fields
**Patient Interested/Patient Interested Item Information:** id(PK), created_at(ts), patient_interested_id(FK→Contact), patient_interested_item_id(FK→Item)
**Other:** last_modified_at(ts)

### ClinicalNote (SDK: `ThcClinicalNote`) — 11 fields
**System Information:** id(PK), unique_id, created_at(ts), last_modified_at(ts)
**Clinical Notes Information:** title, upload(json), content, author_id(FK→Contact), patient_id(FK→Contact), date_created(ts), appointment_id(FK→Appointment)

### Purchase (SDK: `ThcPurchase`) — 26 fields
**Other:** id(PK), contact_id(FK→Contact), coupon_code_id(FK→CouponCode), coupon_id(FK→Coupon), invoice_id(FK→Invoice), order_id(FK→Order), owner_id(FK→User), product_id(FK→Product), subscription_id(FK→Subscription), affiliate_name, coupon_code, coupon_name, created_at(ts), description(longtext), discount(currency), last_modified_at(ts), level1_commission_amount(currency), level2_commission_amount(currency), name, package_id, price(currency), quantity, sku, status(enum:7 values), total_purchase(currency), type(enum:One-time purchase|Payment plan|Subscription|Trial payment)

### Script (SDK: `ThcScript`) — 63 fields
**System Information:** id(PK), trigger, last_note, unique_id, created_at(ts), ip_address, last_activity(ts), last_sms_sent(ts), profile_image(imageUrl), last_email_sent(ts), last_call_logged(ts), last_modified_at(ts), last_sms_received(ts), last_email_received(ts)
**Script Information:** reset(bool), dispenses, doctor_id(FK→Contact), remaining, can_dispense(bool), supply_limit, script_status(enum:8 values), appointment_id(FK→Appointment), thc_to_process(bool), next_dispense_date(ts), last_time_dispensed(ts), last_time_requested(ts), reason_can_t_dispense(enum:8 values)
**PATIENT: Script Payment Form:** patient_script_payment_form_url(url), patient_script_payment_form_visits, patient_script_payment_form_published(bool), patient_script_payment_form_unique_visits
**DOCTOR: Script Edit:** doctor_script_edit_url(url), doctor_script_edit_visits, doctor_script_edit_published(bool), doctor_script_edit_unique_visits
**Treatment Plan:** ai_attempts, treatment_plan
**Halaxy:** repeats, erx_code, valid_until(ts), e_script_link(url), interval_days, manual_script(json), dispense_quantity, create_task_for_erx(bool), dosage_instructions(longtext), additional_instructions(longtext), route_of_administration, doctor_notes_to_pharmacy(longtext)
**Medicine Information:** drug_id(FK→Item), new_item_link(url)
**PAtient Information:** condition(longtext), patient_id(FK→Contact)
**System:** formularyjson(longtext)
**Dispense Revenue:** total_thc_margin, total_retail_sales, total_item_thc_margin
**Create Dispense:** script_in_cart, ai_agent_actions(enum:Add an Unpaid Dispense to the Cart|Create a Paid Dispense), create_paid_dispense(bool), create_dispense_override(bool)
**Doctor Actions:** date_archived(ts), doctor_archive_action(bool)

### Dispense (SDK: `ThcDispense`) — 49 fields
**System Information:** id(PK), owner_id(FK→User), unique_id, created_at(ts), ip_address, last_activity(ts), last_call_logged(ts), last_modified_at(ts), last_email_received(ts)
**Dispense Information:** quantity, script_id(FK→Script), flower_grams, tracking_link(url), dispense_status(enum:11 values), item_retail_gst(currency), item_thc_margin(currency), tracking_number, shipping_company(enum:Startrack), dispensed_item_id(FK→Item), item_retail_price(currency), patient_to_pay_id(FK→Contact), item_wholesale_price(currency), pharmacy_to_dispense_id(FK→Contact), dispense_number_on_script
**PHARMACY: Dispense Add Tracking:** pharmacy_dispense_add_tracking_url(url)
**Batch Information:** date_paid(ts), time_confirmed(ts), time_fulfilled(ts), batch_date_sent, time_set_on_hold(ts), time_tracking_added(ts), date_pharmacy_notified(ts)
**System Fields:** notify_thc(bool), pharmacy_action_button(enum:Dispense|Ready to Ship|Review|Take OFF Hold)
**MAchShip:** eta_datetime(ts), ms_fuel_levy(currency), consignmentid, ms_tax_amount(currency), ms_shipping_cost(currency), consignment_status
**Feedback Information:** rating_text(enum:1⭐ Poor|2⭐ Fair|3⭐ Okay|4⭐ Good|5⭐ Outstanding), rating_number, feedback_comment(longtext), time_rating_given(ts)
**Feedback Request:** feedback_request_url(url), feedback_request_visits, feedback_request_published(bool), feedback_request_unique_visits
**Last time checked CATALYST:** catalyst_checked(bool)

<!-- SCHEMA:END -->
